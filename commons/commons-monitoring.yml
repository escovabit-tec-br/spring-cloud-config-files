# Configuração do actuator para mostrar detalhes
management:
  security:
    enabled: false
  endpoint:
    health:
      show-details: "always"
  endpoints:
    web:
      exposure:
        include: "*"
